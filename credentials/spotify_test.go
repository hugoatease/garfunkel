package credentials

/*func TestSpotifyUserStore(t *testing.T) {
	redisOpts := &redis.Options{
		Addr: "localhost:6379",
	}

	store := NewSpotifyStore(redisOpts)
	credentials := &SpotifyCredentials{
		UserId: "test",
		Token:  "testToken",
	}

	store.Set(credentials)
	currentCreds, _ := store.Get("test")
	fmt.Println("%+v\n", currentCreds)
}*/
